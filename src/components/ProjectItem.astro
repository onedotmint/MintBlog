---
import Badge from './Badge.astro'
import type { ProjectItemData } from '../data/projects'

interface Props {
  project: ProjectItemData
}

const { project } = Astro.props
const projectHref = project.link || (project.detail ? `/projects/${project.detail.slug}/` : undefined)
---

<article class="project-item">
  {projectHref ? (
    <a class="project-link" href={projectHref}>
      <div class="project-headline">
        <h3>{project.name}</h3>
        <span class="arrow" aria-hidden="true">-></span>
      </div>
      <p>{project.description}</p>
    </a>
  ) : (
    <div class="project-link">
      <h3>{project.name}</h3>
      <p>{project.description}</p>
    </div>
  )}

  <div class="tag-row">
    {project.tags.map((tag: string) => <Badge label={tag} />)}
  </div>
</article>

<style>
  .project-item {
    padding: 1rem 0 0;
    border-top: 1px solid var(--border);
  }

  .project-link {
    display: block;
  }

  .project-headline {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
  }

  .project-headline h3 {
    margin-bottom: 0.25rem;
  }

  .arrow {
    color: var(--text-faint);
    font-size: 0.95rem;
  }

  .project-link p {
    margin-bottom: 0.65rem;
  }
</style>
