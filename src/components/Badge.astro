---
interface Props {
  label: string
  href?: string
  active?: boolean
  ariaCurrent?: 'page' | 'step' | 'location' | 'date' | 'time' | boolean
}

const { label, href, active = false, ariaCurrent } = Astro.props
---

{href ? (
  <a
    class:list={{ badge: true, 'badge-active': active }}
    href={href}
    aria-current={ariaCurrent}
  >
    {label}
  </a>
) : (
  <span class:list={{ badge: true, 'badge-active': active }}>
    {label}
  </span>
)}

<style>
  .badge {
    display: inline-flex;
    align-items: center;
    min-height: 1.9rem;
    padding: 0.28rem 0.68rem;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--surface);
    color: var(--text-muted);
    font-size: 0.8rem;
    line-height: 1;
    white-space: nowrap;
  }

  a.badge:hover,
  a.badge:focus-visible {
    border-color: var(--border-strong);
    background: var(--surface-hover);
    color: var(--accent);
  }

  .badge-active {
    border-color: var(--border-strong);
    color: var(--accent);
  }
</style>
