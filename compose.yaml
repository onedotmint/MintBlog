services:
  blog:
    build:
      context: .
      args:
        PUBLIC_SITE_ORIGIN: ${PUBLIC_SITE_ORIGIN:-http://localhost:8080}
    ports:
      - "${BLOG_PORT:-8080}:80"
    restart: unless-stopped
